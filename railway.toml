[build]
builder = "NIXPACKS"
watchPatterns = ["backend/**"]

[deploy]
startCommand = "cd backend && echo '🔧 Creating prisma directory...' && mkdir -p prisma && echo '📊 Running prisma db push...' && npx prisma db push --accept-data-loss --schema=./prisma/schema.prisma && echo '✅ Database ready!' && echo '🚀 Starting server...' && node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Note: Railway needs a persistent volume for SQLite
# Configure in Railway dashboard: Settings > Volumes
# Mount path: /app/backend/prisma

